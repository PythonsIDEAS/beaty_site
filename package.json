{
  "name": "beauty-site",
  "version": "1.0.0",
  "scripts": {
    "vercel-build": "pip install -r requirements.txt && python -m flask db upgrade",
    "start": "python api/index.py"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}